# Netlify config — static site, no build step needed.
[build]
  publish = "."

# Nice URLs / SPA-style fallback is not required since this is one page,
# but this keeps direct hits to /admin/ working.
[[redirects]]
  from = "/admin"
  to = "/admin/"
  status = 301
